{
  "name": "cf-mcp-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "A read-only, fail-closed Cloudflare API MCP bridge running on Cloudflare Workers.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "^0.7.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.7.0",
    "wrangler": "^4.103.0"
  },
  "engines": {
    "node": ">=20"
  }
}
